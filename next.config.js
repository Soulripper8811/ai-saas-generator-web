/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "files.shapes.inc",
      "oaidalleapiprodscus.blob.core.windows.net",
      "replicate.delivery",
    ],
  },
};

module.exports = nextConfig;
